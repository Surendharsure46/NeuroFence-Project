# Runtime dependencies. Install torch first if you need a specific CUDA/CPU build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
transformers>=4.40
safetensors>=0.4
PyYAML>=6.0
numpy>=1.26

# Development
pytest>=8.0
ruff>=0.5

# Optional: only needed by scripts/prepare_model.py --download
# huggingface-hub>=0.23
# Phase 3: desktop app, charts, and PDF reporting
PyQt6>=6.6
matplotlib>=3.8
reportlab>=4.0
